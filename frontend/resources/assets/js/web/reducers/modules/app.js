const initialState = {
    hasNavbar: true,
    hasFooter: true,
    contentLoading: false
}

const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default reducer